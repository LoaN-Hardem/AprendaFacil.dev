// for (let index = 0; index < array.length; index++) {
//     const element = array[index];
// }

// Existem 3 tipos de for em JavaScript: for, for/in e for/of.

const frutas = ["banana", "maca", "uva"];

// Geralmente abreviamos para for(i = 0; i < array.length; i++) aonde o index se chama i
// este é o For:
for (let i = 0; i < frutas.length; i++) {
    console.log(frutas[i]);
}
// ele retorna o array da variavel frutas
// saida esperada: banana, maca, uva 
